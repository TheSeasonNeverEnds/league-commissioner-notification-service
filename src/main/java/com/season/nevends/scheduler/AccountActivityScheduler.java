/*
package com.season.nevends.scheduler;


import org.springframework.beans.factory.annotation.Value;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

@Component
public class AccountActivityScheduler {

    @Scheduled(cron = "${scheduler.account.login-history}") // Execute every day at midnight
    public void checkAccountActivity(){

         // TODO: scheduler runs every day and checks if the last login date for a user is older than 2 months
         // Then, a notification will be sent to the league commissioner(s) notifying them of user inactivity

    }


}
*/